// SET ===========================

const miSet = new Set()

// metodos principales
miSet.add(valor) // añadir valor
miSet.has(valor) // verifica
miSet.delete(valor) // elimina
miSet.clear() // borra todo
miSet.size // numerro de elementos