// Invertir cadena
// Escribe una funcion invertir(cadena) que devuelva la cadena al reves

const numeros = num => num.split('').reverse().join('')

const numeros2 = "jesusa"

console.log(numeros(numeros2))